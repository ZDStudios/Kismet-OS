# Installed via package or vendor script depending on target image
ollama
python3-fastapi
python3-uvicorn
python3-watchdog
python3-requests
python3-pydantic
npm
# Hermes is the preferred agent slot, OpenCode is the coding slot.
# Their upstream packaging is still fluid, so Kismet integrates them as toggleable runtime targets
# and installs them at first boot when a stable package source is configured.
# openclaw installation is layered via first-boot/bootstrap hooks
