# Kismet first boot

This directory holds the first-boot onboarding flow for Kismet OS.

The wizard is designed to run automatically on a fresh install and can also be rerun manually with `kismet-firstboot-wizard`.
